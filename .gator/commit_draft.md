---
message: "Dashboard loop workspace Modules 1–6 — enumeration API, resolver, events, read-only UI, start + prompt, Architect controls, executive summaries"
change-type: feature
significance: notable
decision-tags: [dashboard, loop, api]
agent: claude-opus-4-6
architect: ag
---

# Session Change Log

- Add `_resolve_repo_by_key()` and `_resolve_loop_dir()` resolvers to `gator-dashboard.py` — shared gatekeepers for all loop workspace routes, with path-traversal rejection, symlink rejection, and structural parent containment (`in loop_dir.parents`, not string prefix)
- Add `_dispatch_loop_get()` router and two handlers: `_handle_loop_list()` (sorted by recency, symlinks skipped), `_handle_loop_status()` (allowlisted fields via `_LOOP_STATUS_ALLOWED_KEYS` — unknown/future fields silently dropped)
- Routes use `/api/repo-by-key/<repo_key>/loops/...` prefix, separate from existing name-addressed routes
- Create `tests/test_dashboard_loops.py` with 16 test cases: list empty, list with loops, inaccessible repo 404, two-repo isolation, status found, status not found, status on repo without .gator returns 404, status on repo without loops dir returns 404, dotdot rejected, slash/backslash/null rejected at resolver level, symlink dir rejected, same-prefix sibling rejected, namespace symlink rejected by resolver, namespace symlink returns empty list, symlinks skipped in list, secret fields filtered from status
- Update `scripts-dashboard.md` charter: new resolver and handler entries, Loop connection added
- Review fixes r1: structural containment (BLOCKER), status allowlist (HIGH), events handler deferred to Module 2 (MEDIUM)
- Review fixes r2: namespace-root reparse-point validation — `.gator` and `loops` checked before any loop entry access; `_is_reparse_point()` replaces bare `is_symlink()` everywhere; list handler refuses to iterate when namespace root is a reparse point; loop-entry check split into pre-resolve `is_symlink()` + post-existence `_is_reparse_point()` to avoid false positives on non-existent paths
- Review fixes r3: missing-namespace response taxonomy — `is_dir()` existence checks before `_is_reparse_point()` calls on `.gator` and `loops` so absent namespaces return 404 (FileNotFoundError) instead of 400 (reparse-point error); two new tests for repos missing `.gator` and missing `loops` subdirectory
- Module 2: add `_handle_loop_events()` handler and `/events` route dispatch — returns event timeline via `read_all_events()` from the loop events module; missing events file returns empty array; 3 new tests (events found, no events file, loop not found)
- Module 2 review fix: `events.jsonl` final-component reparse-point check — `is_symlink()` before existence (catches dangling), `_is_reparse_point()` after existence confirmed (catches junctions); prevents symlinked events file from leaking external content; new test for symlinked events file rejection
- Module 3: add `_handle_loop_artifact()` endpoint with `_LOOP_ARTIFACT_ALLOWLIST` — serves allowlisted markdown artifacts (`sketch.md`, `plan.current.md`, `findings.current.md`, round files, decision docs) as `text/plain`; rejects `.tokens.json`, `session.json`, `session.lock`, `events.jsonl`; reparse/symlink check on artifact files
- Module 3: create `views/loop.js` — read-only loop workspace view with loop list sidebar (active/paused sort first), status panel (stage badge, round progress, role state, time remaining, blocked-on-architect card), event timeline, collapsible artifact inspector; 3s polling with terminal-state stop; teardown via `window._gatorRepoTeardown`
- Module 3: update `dashboard.html` (Loop sidebar nav item + script tag), `dashboard.js` (VIEW_META, showView dispatch, loopTab dimming, sidebar click guard), `dashboard.css` (loop workspace layout, cards, badges, timeline, artifact inspector, responsive breakpoint)
- Module 3: update `scripts-dashboard-ui.md` charter with Loop view entry; 6 new artifact endpoint tests (found, not allowed, tokens blocked, not found, round files allowed, symlink rejected)
- Module 3 review fixes r1: artifact filename path traversal — `_LOOP_ARTIFACT_REJECT` regex + strict regexes replace loose prefix matching (BLOCKER); fleet-repo identity collision — `gatorNavToRepo(name, key)` carries `repo_key` through navigation (HIGH); browser-level Playwright coverage — 13 tests in `test_loop_workspace.py` exercising navigation, loop list, status panel, timeline, artifact inspector (content loading + XSS escaping), terminal polling, teardown, and empty state (MEDIUM); seed module `test_loop_seed.py` + harness `seed_loop_fixtures` hook for loop test data
- Module 3 review fixes r2: async stale-render race — `_state.generation` monotonic counter in `loop.js` guards `loadSelectedLoop()` and `pollLoop()` against rendering results from superseded selections (BLOCKER); stable repo identity propagation — `showView()` accepts `repoKeyOverride` parameter, always re-resolves key on name-only navigation, `gatorNavToRepo` passes key through instead of pre-setting state (HIGH); terminal polling test — `data-polling` DOM attribute reflects interval state, test asserts `"1"→"0"` transition then verifies zero API requests after terminal detection (MEDIUM); new test `test_name_only_navigation_resolves_fresh_key` proves URL-based repo switch doesn't retain stale key; total: 14 Playwright tests
- Module 3 review fixes r3: artifact callback race — capture `_state.generation` and loop ID at toggle click time, check generation match + `content.isConnected` before mutating DOM in the fetch callback (BLOCKER); mount resurrection — `_state.mountId` counter separates mount identity from selection identity, entry point checks `mountId` after every await and returns before installing timer/teardown hook if mount was torn down, teardown increments `mountId` (HIGH); `_select_loop_card` test helper clears h3 atomically before clicking to distinguish fresh render from stale; duplicate-name fleet identity test — intercepts `/api/data` to rename beta→alpha, clicks the beta-key row, verifies all Loop API requests carry beta's key not alpha's (MEDIUM); mount resurrection test — holds initial loops fetch via threading gate, navigates away, releases, verifies zero poll requests after 4s; total: 16 Playwright tests
- Module 4: extract `init_loop()` from `start_loop()` in `host.py` — creates loop on disk without entering the watch loop, accepts optional `repo_root` to bypass `find_gator_root()` for dashboard reuse; returns `(loop_id, loop_dir)`
- Module 4: add platform-aware non-blocking file locking — `host.lock` (per-loop exclusive, proves watcher ownership) and `start.lock` (per-repo, enforces one-active-loop-per-repo during start sequence); `acquire_host_lock()`, `release_host_lock()`, `acquire_start_lock()`, `release_start_lock()`, `write_host_metadata()`; `msvcrt.locking(LK_NBLCK)` on Windows, `fcntl.flock(LOCK_EX|LOCK_NB)` on POSIX
- Module 4: add `find_active_loop(loops_base)` — scans for non-terminal sessions, returns loop_id or None
- Module 4: rewrite `start_loop()` to use lock flow — `start.lock` → scan → `init_loop()` → `host.lock` → release `start.lock` → `watch_loop()` → release `host.lock`; update `watch_loop()` to accept optional `host_lock_fd` for diagnostic metadata
- Module 4: add `resolve_token(token, loop_dir=None)` — when `loop_dir` provided, skips `find_gator_root()` and validates token's loop_id matches directory name
- Module 4: add `loop_dir=None` parameter to `handle_pause()`, `handle_interject()`, `handle_end()`, `handle_unblock()` in `submit.py` — passed through to `resolve_token()` for dashboard-side calls
- Module 4: add `_LOOP_HOSTS` host registry with `_HostEntry`, `_run_watcher()` daemon thread wrapper (nonce-guarded cleanup), and `_adopt_orphaned_loops()` startup scan in `gator-dashboard.py`
- Module 4: add `_dispatch_loop_post()` router, `_handle_loop_start()` (validates feature/sketch, acquires `start.lock`, scans for active, calls `init_loop()`, acquires `host.lock`, starts watcher thread, returns 201), and `_handle_loop_prompt()` (draftor/reviewer only, never architect, `Cache-Control: no-store` on all paths, 410 for terminal loops)
- Module 4: update `ensure_loops_gitignore()` to include `host.lock` and `start.lock` rules
- Module 4: 16 new API tests — `TestLoopStart` (9: happy path, missing/empty feature, missing/invalid sketch, traversal sketch, active loop conflict, cross-repo isolation), `TestLoopPrompt` (6: draftor/reviewer prompts, architect rejected, invalid role, terminal 410, not found), `TestLoopStartCrossRepo` (1: two repos start independently)
- Module 4: update `scripts-loop.md` charter — add `init_loop()` entry, locking entries, `find_active_loop()`, update `start_loop()`/`watch_loop()` signatures, update `resolve_token()` for `loop_dir`, add Dashboard connection; update `scripts-dashboard.md` charter — add POST dispatch/handler/registry entries
- Module 4 review fix (BLOCKER): registry-before-start ordering — publish `_HostEntry` to `_LOOP_HOSTS` before `t.start()` in both `_handle_loop_start()` and `_adopt_orphaned_loops()`; if `start()` fails, nonce-guarded cleanup removes entry and releases fd; new test verifies immediate-exit watcher leaves empty registry and released lock
- Module 4 review fix (HIGH): host-ownership and start-exclusivity test coverage — `TestHostOwnership` (held lock not adopted, terminal loop skipped, active orphan adopted with fd), `TestWatcherLifecycle` (immediate exit cleanup, nonce-mismatch preserves replacement), `TestConcurrentStart` (two threads race, one 201 and one 409)
- Module 4 review fix r2 (HIGH): replace same-process lock tests with subprocess-based cross-process isolation — `test_cross_process_host_lock_not_adopted` (subprocess holds `host.lock`, adoption skips), `test_cross_process_start_lock_blocks_dashboard` (subprocess holds `start.lock`, start returns 409), `test_cross_process_start_lock_serializes` (two subprocesses race, exactly one acquires), `test_start_lock_released_after_success` and `test_start_lock_released_on_active_conflict` (lock reacquirable after success/error paths)
- Module 4 review fix (MEDIUM): numeric validation for `max_rounds` (int, 1–20) and `turn_timeout` (int, 30–3600) in `_handle_loop_start()`; booleans explicitly rejected; 8 new `TestLoopStartNumericValidation` tests
- Module 5: add `_resolve_architect_token()` shared gatekeeper — resolves `loop_dir` and reads architect token from `.tokens.json`; returns None with error response on failure
- Module 5: add `_handle_loop_pause()`, `_handle_loop_interject()`, `_handle_loop_unblock()`, `_handle_loop_end()` — each delegates to the corresponding `submit.py` handler with `loop_dir=loop_dir`; state machine rejections returned as 409; interject requires non-empty message (400)
- Module 5: extend `_dispatch_loop_post()` to route `/pause`, `/interject`, `/unblock`, `/end` via action suffix matching
- Module 5: add Architect controls UI to `loop.js` — `renderControls()` shows contextual buttons (Pause/Interject/End for active, Unblock/End for paused, none for terminal); inline message input with confirm/cancel; `postAction()` helper with anti-CSRF header; re-polls on success
- Module 5: add Architect controls CSS (control bar, contextual button colors, inline input area with error state)
- Module 5: 14 new `TestArchitectControls` tests — pause active/terminal/no-message, interject with-message/no-message/terminal, unblock paused/blocked/active, end active/paused/terminal, loop-not-found, malicious loop_id
- Module 5: update `scripts-dashboard.md` charter — add `_resolve_architect_token()` and control handler entries; update `scripts-dashboard-ui.md` — add `renderControls()` and control endpoint references
- Module 5 whiteboard fix (HIGH): `postAction()` now checks `resp.ok` — non-2xx responses return `{ _failed: true, error }` instead of silently discarding failures; confirm handler renders `.loop-ctrl-error` message and preserves input area for retry; `clearCtrlError()` helper removes error on new button click or cancel
- Module 5 whiteboard fix (HIGH): add `TestResolveTokenExplicitLoopDir` (4 tests) in `test_loop.py` — explicit loop_dir resolution, mismatched token/loop rejection, handler role-rejection tests for model-cannot-interject and reviewer-cannot-execute-architect-actions; add `TestArchitectControlsErrorPayload` and `TestArchitectControlsCrossRepo` in `test_dashboard_loops.py` — terminal rejection error message, multi-repo control isolation
- Module 5 whiteboard fix (MEDIUM): add 4 Playwright browser tests — `test_active_loop_shows_controls`, `test_terminal_loop_shows_no_controls`, `test_control_error_displayed_on_failure` (route interception for 409), `test_pause_then_shows_unblock_controls` (mock pause + status refresh)
- Module 6: add `extractSummary()` function in `loop.js` — case-insensitive regex on `## Executive Summary`, extracts to next `##` or EOF, truncates at 500 chars with ellipsis; exported as `window.GatorViews._extractSummary` for test access
- Module 6: update `renderArtifacts()` — proactively fetches `plan.*` and `findings.*` artifacts for summary extraction; renders inline `.loop-summary-text` for found summaries or `.loop-summary-absent` fallback for artifacts without a summary
- Module 6: add executive summary CSS styles — `.loop-artifact-summary`, `.loop-summary-text` (border-left accent, 12px), `.loop-summary-absent` (italic, muted, 11px)
- Module 6: add `## Executive Summary` section to plan and findings templates in `loop-artifact-formats.md` and loop protocol `gator-loop-protocol.md` (both twin pairs byte-identical)
- Module 6: update `test_loop_seed.py` — add executive summary content to active loop's `plan.current.md`; fix turn deadline to far-future `2099-12-31T23:59:59+00:00` to prevent host watcher timeout interference
- Module 6: add 7 Playwright tests — 5 `extractSummary` unit tests (exact heading, case insensitive, extra whitespace, no heading returns null, truncation at 500 chars) + 2 integration tests (summary renders in artifact inspector, absent summary shows fallback)
- Module 6: update `scripts-dashboard-ui.md` charter — add executive summary extraction entry; document inspector-only placement as deliberate design decision (plan specified timeline, inspector provides better context adjacency)
- Module 6 whiteboard fix (HIGH): add executive summary requirement to all producer paths — `render_entry_content()` in `entry_points.py`, `/loop-join` command template (both `.claude/commands/` and `src/.../templates/gator-starter/commands/` copies, verified byte-identical), and live entry points (CLAUDE.md, AGENTS.md, GEMINI.md managed blocks refreshed); 4 new `TestExecutiveSummaryProducerPaths` content-pin tests in `test_loop.py` + 1 new `test_executive_summary_requirement_present` in `test_gatorize.py`
- Module 6 whiteboard fix (HIGH): summary placement documented as deliberate UX change in `scripts-dashboard-ui.md` charter — inspector-only (next to artifact) instead of timeline event cards; avoids fragile event-to-artifact association
- Module 6 whiteboard fix (MEDIUM): `SUMMARY_RE` regex updated to `^\s*##\s+...` — tolerates leading whitespace before H2 marker; new Playwright test `test_extractSummary_leading_whitespace` verifies; fallback test `test_absent_summary_shows_fallback` now requires at least one `.loop-summary-absent` element (was guarded by `if absent_texts` which passed vacuously); seed data updated with `findings.current.md` and `findings.round-1.md` that have content but no executive summary section
- Module 6 whiteboard fix (HIGH): timeline executive summaries — `renderTimeline()` now maps `draft_submitted`/`review_submitted` events to their round artifacts via `_EVENT_ARTIFACT_MAP`, fetches the artifact text, extracts the summary, and renders it inside the timeline event card as `.loop-timeline-summary-text` or `.loop-timeline-summary-absent` fallback; each card includes a "View full artifact" link (`.loop-timeline-artifact-link`) that scrolls to and opens the corresponding artifact in the inspector; CSS styles for timeline summary, fallback, and link; seed data updated with `review_submitted` event and round artifact files (`plan.round-1.md`, `findings.round-1.md`); 4 new Playwright tests (timeline draft summary, artifact link presence, absent summary fallback, link opens inspector); charter updated to document both inspector and timeline summary rendering
- Module 6 whiteboard fix r3 (BLOCKER): replace derived `_EVENT_ARTIFACT_MAP` with explicit `artifact_path` field on submission events — `handle_submit_draft()` adds `artifact_path: plan.round-{R}.md` to `draft_submitted` events, `handle_submit_review()` adds `artifact_path: findings.round-{R}.md` to `revision_requested`/`plan_approved`/`max_rounds_exceeded` events (R = pre-advance round); `_artifactForEvent()` in `loop.js` simplified to `return ev.artifact_path || null`; seed data corrected from `review_submitted` to `revision_requested` with proper artifact paths
- Module 6 whiteboard fix r3 (HIGH): fix test assertions for round-0 based naming — `create_session` sets `round: 0`, so first draft creates `plan.round-0.md` not `plan.round-1.md`; `TestEventArtifactPath` (4 tests) exercises real submit handlers across round 0, revision-requested, plan-approved, and multi-round immutability
